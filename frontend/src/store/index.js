import { createStore } from "vuex";
import auth from "@/store/modules/auth.js";

export default createStore({
  state() {
    return {};
  },
  mutations: {},
  actions: {},
  modules: {
    auth,
  },
});
